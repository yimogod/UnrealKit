<Window x:Class="UnrealKit.Desktop.Views.OperationLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="UnrealKit 操作日志" Height="560" Width="900" MinHeight="320" MinWidth="560">
    <Grid Background="#FFF7F8FA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Background="White" BorderBrush="#FFE2E6EA" BorderThickness="0,0,0,1" Padding="16,12">
            <DockPanel LastChildFill="False">
                <TextBlock Text="操作日志" Style="{StaticResource SectionTitleTextStyle}" VerticalAlignment="Center" />
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <CheckBox Content="自动滚动到最新"
                              IsChecked="{Binding AutoScroll, RelativeSource={RelativeSource AncestorType=Window}}"
                              VerticalAlignment="Center" Margin="0,0,16,0" />
                    <Button Content="保存…" Click="SaveLogs_Click" IsEnabled="{Binding HasOperationLogs}" Padding="14,7" Margin="0,0,10,0" />
                    <Button Content="清空" Command="{Binding ClearOperationLogsCommand}" Padding="14,7" />
                </StackPanel>
            </DockPanel>
        </Border>

        <!--
            日志量可达数千条，必须启用虚拟化：Grid 行高为 * 让 DataGrid 拿到有界高度，
            外面不能再套 ScrollViewer，否则高度变为无限、虚拟化失效。
        -->
        <DataGrid x:Name="LogGrid"
                  Grid.Row="1"
                  ItemsSource="{Binding OperationLogs}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#FFEDF1F5"
                  EnableRowVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  BorderThickness="0"
                  Margin="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="时间" Binding="{Binding Time}" Width="80" />
                <DataGridTextColumn Header="分类" Binding="{Binding Category}" Width="110" />
                <DataGridTextColumn Header="内容" Binding="{Binding Message}" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap" />
                            <Setter Property="FontFamily" Value="Consolas" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="2" Background="White" BorderBrush="#FFE2E6EA" BorderThickness="0,1,0,0" Padding="16,8">
            <TextBlock Text="{Binding OperationLogCount}" Style="{StaticResource HintTextStyle}" />
        </Border>
    </Grid>
</Window>
