<Window x:Class="UnrealKit.Desktop.Views.StaticMeshStatsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="StaticMesh 资产统计"
        Height="720" Width="1100" MinHeight="400" MinWidth="640"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 标题栏：SM 基本信息 -->
        <Border Grid.Row="0" Background="White" BorderBrush="#FFE2E6EA" BorderThickness="0,0,0,1" Padding="16,10">
            <DockPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
                    <TextBlock x:Name="StatsCountText" Style="{StaticResource HintTextStyle}" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel>
                    <TextBlock x:Name="TitleText" Text="StaticMesh 资产统计"
                               Style="{StaticResource SectionTitleTextStyle}"
                               TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" />
                    <TextBlock x:Name="PathText" Style="{StaticResource HintTextStyle}"
                               FontFamily="Consolas" FontSize="10"
                               TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"
                               Margin="0,3,0,0" />
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- 主体：SM 属性 + 三个 DataGrid -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- SM 属性行 -->
            <Border Grid.Row="0" Background="#FFF8F9FA" BorderBrush="#FFE2E6EA" BorderThickness="0,0,0,1" Padding="16,8">
                <WrapPanel Orientation="Horizontal">
                    <StackPanel Orientation="Horizontal" Margin="0,0,24,0">
                        <TextBlock Text="LOD：" Style="{StaticResource HintTextStyle}" VerticalAlignment="Center" />
                        <TextBlock x:Name="LodCountText" FontFamily="Consolas" FontSize="11" VerticalAlignment="Center" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,24,0">
                        <TextBlock Text="材质槽：" Style="{StaticResource HintTextStyle}" VerticalAlignment="Center" />
                        <TextBlock x:Name="MaterialCountText" FontFamily="Consolas" FontSize="11" VerticalAlignment="Center" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,24,0">
                        <TextBlock Text="顶点：" Style="{StaticResource HintTextStyle}" VerticalAlignment="Center" />
                        <TextBlock x:Name="VertexCountText" FontFamily="Consolas" FontSize="11" VerticalAlignment="Center" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,24,0">
                        <TextBlock Text="三角面：" Style="{StaticResource HintTextStyle}" VerticalAlignment="Center" />
                        <TextBlock x:Name="TriangleCountText" FontFamily="Consolas" FontSize="11" VerticalAlignment="Center" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                        <TextBlock Text="Chunk：" Style="{StaticResource HintTextStyle}" VerticalAlignment="Center" />
                        <TextBlock x:Name="ChunkText" FontFamily="Consolas" FontSize="11" VerticalAlignment="Center" />
                    </StackPanel>
                </WrapPanel>
            </Border>

            <!-- 三个列表：Grid 分三列 -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" MinWidth="200" />
                    <ColumnDefinition Width="5" />
                    <ColumnDefinition Width="*" MinWidth="180" />
                    <ColumnDefinition Width="5" />
                    <ColumnDefinition Width="*" MinWidth="200" />
                </Grid.ColumnDefinitions>

                <!-- Materials -->
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="#FFF8F9FA" BorderBrush="#FFE2E6EA" BorderThickness="0,0,0,1" Padding="12,6">
                        <TextBlock x:Name="MatHeaderText" Text="Material（0）"
                                   FontWeight="SemiBold" FontSize="12" />
                    </Border>
                    <DataGrid Grid.Row="1" x:Name="MaterialsGrid"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column"
                              CanUserResizeRows="False"
                              SelectionMode="Single"
                              FontSize="11"
                              RowHeight="22"
                              BorderThickness="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name"        Binding="{Binding Name}"                   Width="*" />
                            <DataGridTextColumn Header="BlendMode"   Binding="{Binding BlendMode}"              Width="80" />
                            <DataGridTextColumn Header="Shading"     Binding="{Binding ShadingModel}"           Width="80" />
                            <DataGridTextColumn Header="Textures"    Binding="{Binding ReferencedTextureCount}" Width="55" />
                            <DataGridTextColumn Header="TwoSided"    Binding="{Binding TwoSided}"               Width="60" />
                            <DataGridTextColumn Header="Chunk"       Binding="{Binding PakChunkId}"             Width="44" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>

                <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch"
                              Background="#FFE2E6EA" ResizeBehavior="PreviousAndNext" />

                <!-- MatInstances -->
                <Grid Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="#FFF8F9FA" BorderBrush="#FFE2E6EA" BorderThickness="0,0,0,1" Padding="12,6">
                        <TextBlock x:Name="MatInstHeaderText" Text="MatInstance（0）"
                                   FontWeight="SemiBold" FontSize="12" />
                    </Border>
                    <DataGrid Grid.Row="1" x:Name="MatInstancesGrid"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column"
                              CanUserResizeRows="False"
                              SelectionMode="Single"
                              FontSize="11"
                              RowHeight="22"
                              BorderThickness="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name"      Binding="{Binding Name}"                   Width="*" />
                            <DataGridTextColumn Header="Parent"    Binding="{Binding ParentName}"             Width="*" />
                            <DataGridTextColumn Header="TexParams" Binding="{Binding TextureParameterCount}"  Width="66" />
                            <DataGridTextColumn Header="Chunk"     Binding="{Binding PakChunkId}"             Width="44" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>

                <GridSplitter Grid.Column="3" Width="5" HorizontalAlignment="Stretch"
                              Background="#FFE2E6EA" ResizeBehavior="PreviousAndNext" />

                <!-- Textures -->
                <Grid Grid.Column="4">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="#FFF8F9FA" BorderBrush="#FFE2E6EA" BorderThickness="0,0,0,1" Padding="12,6">
                        <TextBlock x:Name="TexHeaderText" Text="Texture（0）"
                                   FontWeight="SemiBold" FontSize="12" />
                    </Border>
                    <DataGrid Grid.Row="1" x:Name="TexturesGrid"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column"
                              CanUserResizeRows="False"
                              SelectionMode="Single"
                              FontSize="11"
                              RowHeight="22"
                              BorderThickness="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name"     Binding="{Binding Name}"           Width="*" />
                            <DataGridTextColumn Header="Size"     Binding="{Binding Resolution}"     Width="75" />
                            <DataGridTextColumn Header="Format"   Binding="{Binding Format}"         Width="66" />
                            <DataGridTextColumn Header="Est. MB"  Binding="{Binding EstimatedSizeMB}" Width="56" />
                            <DataGridTextColumn Header="Chunk"    Binding="{Binding PakChunkId}"     Width="44" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Grid>
        </Grid>

        <!-- 底部状态栏 -->
        <Border Grid.Row="2" Background="White" BorderBrush="#FFE2E6EA" BorderThickness="0,1,0,0" Padding="16,6">
            <TextBlock x:Name="FooterText" Style="{StaticResource HintTextStyle}" />
        </Border>
    </Grid>
</Window>
