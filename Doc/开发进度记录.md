# UnrealKit 开发进度记录

最后更新：2026-08-05

本文用于在清理 Agent 上下文后恢复开发。实现范围以 `Doc/第一阶段核心可用版本详细TODO.md` 为准。

## 当前状态

- 当前实现目录：`UnrealKit/`
- 解决方案：`UnrealKit/UnrealKit.sln`
- 目标框架：.NET 9；桌面端为 WPF（Windows）
- 当前阶段：M2（ADB 基础设施与启动参数）
- M0、M1 已完成；M2 的通用进程执行器与 ADB 设备基础服务已完成；启动参数服务尚未开始。

## 已完成

### M0：解决方案与基础设施

- 已建立四项目结构：
  - `UnrealKit.Core`：领域模型与业务服务。
  - `UnrealKit.Cli`：命令行适配层，引用 Core。
  - `UnrealKit.Desktop`：WPF 壳与工作流导航，引用 Core。
  - `UnrealKit.Tests`：xUnit 测试，引用 Core。
- `UnrealKit/Directory.Build.props` 统一启用 Nullable、隐式 using、最新语言版本和将警告视为错误。
- 已提供共享操作基础设施：
  - `Operations/OperationInfrastructure.cs`：`OperationProgress`、`LogEvent`、`IOperationLogger`。
  - `Runtime/AppVersionInfo.cs`：应用版本、构建时间、可选 Git commit。
- `.gitignore` 已调整为适配 Unreal Engine 与 .NET/WPF：保留 `.sln` / `.csproj`，忽略 `bin/`、`obj/`、`.vs/`、测试结果、WPF 生成物与本地 SDK 状态。

### M1：`.ukit` 工程与配置

- Core 中已实现工程模型与服务：
  - `Projects/ProjectModels.cs`
  - `Projects/IniDocument.cs`
  - `Projects/ProjectService.cs`
- 支持创建、打开、校验 `.ukit` 工程。
- 创建工程会生成：
  - `<Name>.ukit`（UTF-8 INI，格式版本为 1）
  - `Config/DefaultGame.ini`
  - `Content/`、`Saved/`、`Intermediate/`
- 非空目标目录会被拒绝，避免静默覆盖。
- 校验会报告工程格式版本、必需目录、工程名称和缺失配置的结构化诊断。
- CLI 已支持：
  - `project create <directory> --name <name>`
  - `project info <project.ukit> [--format json]`
  - `project validate <project.ukit>`
- WPF 已建立无业务逻辑的导航壳：工程、设备、启动参数、采集、解析、结果、日志与设置。

### M2：已完成部分

#### 安全进程执行器

- 文件：
  - `UnrealKit.Core/Processes/ProcessExecutionModels.cs`
  - `UnrealKit.Core/Processes/IProcessRunner.cs`
  - `UnrealKit.Core/Processes/ProcessRunner.cs`
- `ProcessRunner` 使用 `ProcessStartInfo.ArgumentList`，不通过拼接 shell 命令传递参数。
- 可记录/返回：退出码、stdout、stderr、开始/结束时间和耗时。
- 支持取消令牌、超时、超时/取消时终止整个进程树、进度与结构化日志。

#### ADB 设备与命令服务

- 文件：
  - `UnrealKit.Core/Adb/AdbModels.cs`
  - `UnrealKit.Core/Adb/AdbDeviceParser.cs`
  - `UnrealKit.Core/Adb/IAdbService.cs`
  - `UnrealKit.Core/Adb/AdbService.cs`
- `adb devices -l` 解析支持制表符与空格分隔，识别：序列号、`device/offline/unauthorized`、产品、型号、设备名及 USB/Wi-Fi 类型。
- 已实现：`version`、`devices -l`、server 启停、Wi-Fi connect/disconnect、`tcpip`。
- 已提供后续采集可复用的方法：启动应用、push、pull、远端删除、`dumpsys meminfo`。
- 任何设备相关命令都要求显式序列号，并统一传入 `-s <serial>`。
- 本地路径、Android 包名、远端 Unix 绝对路径均做基础校验。
- ADB 非零退出码抛出 `AdbCommandException`，保留退出码、stdout 和 stderr。

#### CLI ADB 基础命令

- `UnrealKit.Cli/Program.cs` 已以 UTF-8 重写，修复了移动目录后出现的中文乱码。
- 已支持：
  - `adb version [--adb-path <path>]`
  - `adb devices [--adb-path <path>]`
  - `adb connect <host:port> [--adb-path <path>]`
  - `adb disconnect <host:port> [--adb-path <path>]`
- ADB 不存在时 CLI 会输出可读错误并返回退出码 `1`。

## 测试与验证

2026-08-05 最近验证结果：

```powershell
$env:DOTNET_CLI_HOME = (Join-Path (Resolve-Path .\UnrealKit) '.dotnet-cli')
dotnet test .\UnrealKit\UnrealKit.Tests\UnrealKit.Tests.csproj --no-restore
dotnet build .\UnrealKit\UnrealKit.sln --no-restore
```

- 单元测试：11/11 通过。
- 全解决方案构建：成功，0 警告、0 错误。
- 已手工验证错误路径：

```powershell
dotnet run --project .\UnrealKit\UnrealKit.Cli\UnrealKit.Cli.csproj --no-build -- adb devices --adb-path definitely-missing-adb
```

该命令输出“无法启动外部进程”类诊断，CLI 返回退出码 `1`。

## 当前未提交改动

截至 2026-08-05，`git status --short` 显示的当前未提交实现包括：

- 修改：`UnrealKit/UnrealKit.Cli/Program.cs`
- 新增：`UnrealKit/UnrealKit.Core/Adb/`
- 新增：`UnrealKit/UnrealKit.Core/Processes/`
- 新增：`UnrealKit/UnrealKit.Tests/AdbServiceTests.cs`
- 新增：`UnrealKit/UnrealKit.Tests/ProcessRunnerTests.cs`

提交前应再次运行上方的测试与构建命令。不要提交 `bin/`、`obj/` 或 `UnrealKit/.dotnet-cli/`。

## 下一步：完成 M2 启动参数服务

建议按以下顺序继续：

1. 在 `UnrealKit.Core/Launch`（或 `UnrealKit.Core/Adb` 下）定义启动参数预设模型：名称、内容、描述、是否可叠加。
2. 加入首版预设：LLM、LLM CSV、OpenGL、Vulkan、Trace Default、Trace All、Trace Network、Trace Memory、No Update；参数内容需参照旧 Python 工具兼容规则，不要凭空猜测。
3. 实现 `LaunchParameterService`：组合预设和自定义参数、生成本地临时 `uecommandline.txt`、调用 `IAdbService.PushFileAsync` 推送、调用 `DeleteRemoteFileAsync` 删除、调用 `StartApplicationAsync` 启动。
4. 推送/删除/启动请求必须显式包含设备序列号、包名、Activity、远端路径；GUI 后续必须在危险操作前显示这些目标。
5. CLI 增加对应的显式命令；WPF 再接入“启动参数”页面。
6. 为参数组合、临时文件清理、push/delete/start 调用顺序、无效路径/缺失序列号新增单元测试。

## 已知限制

- `ProcessRunner` 当前在进程结束后一次性读取 stdout/stderr；TODO 中要求的“流式输出到 GUI/CLI”尚未实现。
- ADB 路径当前只接受构造参数或 PATH 中的 `adb`；尚未实现从项目设置、环境变量和 PATH 的完整优先级诊断。
- `AdbDeviceSelectionException` 已定义，但“唯一可用设备时由调用方明确确认后选择”的选择策略尚未实现。
- WPF 仍是导航/占位壳，未接入工程、ADB 或启动参数服务。
- Capture 归档、解析、导出、Agent 分析均未开始。
